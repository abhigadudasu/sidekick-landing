# Sidekick Social — landing page

Static public marketing page for Sidekick Social (used as the company website for platform developer-profile review). Single file: `index.html`. Deployed via Vercel (preset: Other / static).
